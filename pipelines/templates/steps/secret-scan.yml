steps:
  - bash: |
      set -euo pipefail
      docker run --rm \
        --volume "$(Build.SourcesDirectory):/repository" \
        ghcr.io/gitleaks/gitleaks:v8.30.1 \
        git --redact --verbose /repository
    displayName: Scan Git history for secrets with Gitleaks
