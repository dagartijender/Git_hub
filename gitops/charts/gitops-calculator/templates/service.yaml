apiVersion: v1
kind: Service
metadata:
  name: {{ include "gitops-calculator.fullname" . }}
  labels:
    {{- include "gitops-calculator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gitops-calculator.selectorLabels" . | nindent 4 }}
